<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/logo"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:targetApi="31">

        <!-- Actividad Principal -->
        <activity android:name="android.example.quantummind.presentation.LoginActivity"
                  android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- Otras Actividades -->
        <activity android:name="android.example.quantummind.presentation.AchievementsActivity" />
        <activity android:name="android.example.quantummind.presentation.RegisterActivity" />
        <activity android:name="android.example.quantummind.presentation.MainActivity" />
        <activity android:name="android.example.quantummind.presentation.UserProfileActivity" />
        <activity android:name="android.example.quantummind.presentation.EditProfileActivity" />
        <activity android:name="android.example.quantummind.presentation.LevelActivity" />
        <activity android:name="android.example.quantummind.presentation.RankingActivity" />
        <activity android:name="android.example.quantummind.presentation.ImageZoomActivity" />

    </application>

</manifest>